# Opacity

opacity-rule = [
  # Alacritty
  "96:class_g = 'Alacritty' && focused",
  "96:class_g = 'Alacritty' && !focused",

  # Discord
  "95:class_g = 'discord' && focused",
  "95:class_g = 'discord' && !focused",

];

